# Core analysis. Versions are those used to produce the results in the paper.
numpy==2.2.5
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.7.0
lightgbm>=4.0
matplotlib==3.10.9
pyarrow>=15.0
requests>=2.31
joblib>=1.3

# LEAR benchmark: the reference implementation by Lago, Marcjasz, De Schutter and Weron.
# Install separately, without dependencies, so that TensorFlow is not pulled in:
#   pip install --no-deps --ignore-requires-python git+https://github.com/jeslago/epftoolbox.git
# The LEAR model does not use TensorFlow; only the package's DNN module does, and we do not
# use that module. No package source is modified.
